#include <stdio.h>

typedef struct{
    char primative;
    char* name;
    char* description;
    char* parameter;
}dataType;

int main(int argc, char* argv[]){
    printf("there are %i arguments\n", argc);
    printf("the second argument is %s\n", argv[1]);
    return 0;
}

void callType(char* dataType){
    printf("Define: %s/n");
    if(dataType == 0);
}

void fileIO(){
    
}
