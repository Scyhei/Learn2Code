#include <stdio.h>

//The entry point main() does not require argc & argv because they are not being called in the terminal
int main(){
    //The following printf prints the given string but does not create a new line
    printf("Hello, World!");
    //program ends
    return 0;
}